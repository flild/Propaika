@page
@model Propaika_main_app.Pages.Admin.Prices.DeleteModel
@{
    ViewData["Title"] = "Удалить услугу";
}
<h2 class="text-danger">Удаление услуги</h2>

<div class="alert alert-warning mt-3">
    Вы действительно хотите удалить эту услугу из прайс-листа?
</div>

<dl class="row mt-3">
    <dt class="col-sm-3">Модель</dt>
    <dd class="col-sm-9">@Model.PriceItem.DeviceModel?.Name</dd>

    <dt class="col-sm-3">Услуга</dt>
    <dd class="col-sm-9">@Model.PriceItem.ServiceName</dd>

    <dt class="col-sm-3">Цена</dt>
    <dd class="col-sm-9">@Model.PriceItem.Cost.ToString("N0") ₽</dd>
</dl>

<form method="post" class="mt-3">
    <button type="submit" class="btn btn-danger">Да, удалить</button>
    <a asp-page="./Index" class="btn btn-secondary">Отмена</a>
</form>